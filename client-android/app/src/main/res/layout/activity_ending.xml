<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp"
    android:background="@drawable/gameplay_background"
    android:fitsSystemWindows="true"
    android:clipToPadding="false">

    <!-- Контейнер с кнопками концовок -->
    <LinearLayout
        android:id="@+id/endingButtonsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:gravity="center">

        <Button
            android:id="@+id/buttonAllSurvived"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Спаслись все"
            android:background="@drawable/button_back_bg"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:fontFamily="@font/panton_rust_scr_sm_bold_gr"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/buttonSomeoneDied"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Кто-то из нас пал"
            android:background="@drawable/button_back_bg"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:fontFamily="@font/panton_rust_scr_sm_bold_gr"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/buttonAlone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Я остался один"
            android:background="@drawable/button_back_bg"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:fontFamily="@font/panton_rust_scr_sm_bold_gr"
            android:layout_marginBottom="12dp" />

        <!-- Альтернативная концовка с красным фоном -->
        <Button
            android:id="@+id/buttonAltEnding"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Альтернативная концовка"
            android:backgroundTint="@android:color/holo_red_light"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:fontFamily="@font/panton_rust_scr_sm_bold_gr"
            android:layout_marginBottom="12dp" />
    </LinearLayout>

    <!-- Кнопка Назад -->
    <Button
        android:id="@+id/buttonBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="назад"
        android:background="@drawable/button_back_bg"
        android:fontFamily="@font/panton_rust_scr_sm_bold_gr"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:alpha="0.9"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="24dp" />
</RelativeLayout>